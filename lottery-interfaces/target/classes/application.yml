server:
  port: 8080

spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://101.200.88.237:3306/lottery?useSSL=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:/mybatis/mapper/*.xml
  config-location:  classpath:/mybatis/config/mybatis-config.xml

# Dubbo 广播方式配置
dubbo:
  application:  #配置应用名称和版本
    name: LotterySystem
    version: 1.0.0
  registry:     #注册地址
    address: N/A #multicast://224.5.6.7:1234
  protocol:   #配置和通信协议和端口
    name: dubbo
    port: 20880
  scan:  #相当于Spring中自动扫描包的地址，可以把此包下的所有rpc接口都注册到服务中
    base-packages: cn.itedus.lottery.rpc
